#include <stdio.h>

// Use a stack to order a sequence of floating point numbers in increasing order of their values. The
// values are read one by one from the keyboard. Every new number is saved in the stack. Numbers
// are read until the user enters ‘#’. Then, the values stored in the stack are displayed in the desired
// order. 

int main(){
    
    return 0;
}